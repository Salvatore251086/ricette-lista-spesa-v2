name: Auto import (manual)

on:
  workflow_dispatch: {}

jobs:
  import-data:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dipendenze
        run: npm ci

      # QUI richiami SOLO script di import che usano TUOI dati
      # Esempio: export da Strapi o da un dataset open
      # Sostituisci la riga sotto quando avrai lo script giusto
      - name: Import placeholder
        run: |
          echo "Qui richiamerai il tuo script di import (es. npm run export:recipes)"
          echo "Per ora non fa nulla, è solo un gancio pronto."

      - name: Mostra cambi
        run: git diff || true
